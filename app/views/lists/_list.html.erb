<div data-controller="list" data-list-id-value="<%= list.id %>" data-list-stream-tag-value="<%= list.stream_tag %>" class="flex-1 max-w-lg min-w-80 rounded-md m-2 bg-gray-100 p-4">

  <div class="flex space-x-3">
    <form data-action="list#updateName focusout->list#updateName" class="flex-grow">
      <input id="listName" data-list-target="name" placeholder="Fill the name here..." type="text" maxlength="24" class="name" value="<%= list.name %>">
    </form>

    <%= render 'lists/dropdown' do %>
      <form id="deleteList" data-action="list#delete" class="block px-4 py-2 leading-5 text-red-700 hover:bg-gray-100 hover:text-red-900 focus:outline-none focus:bg-gray-100 focus:text-red-900">
        <button type="submit" class="text-left font-semibold w-full">Delete</button>
      </form>
    <% end %>
  </div>
</div>

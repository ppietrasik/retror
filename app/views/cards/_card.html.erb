<div data-controller="card" data-card-id-value="<%= card.id %>" data-card-stream-tag-value="<%= card.stream_tag %>" class="w-full rounded-md bg-white shadow-md p-2" >
  
  <div class="flex space-x-1">
    <textarea data-card-target="note" maxlength="255" class="p-2 max-h-48 rounded-md resize-none w-full border-none focus:ring-2 focus:ring-blue-600"><%= card.note %></textarea>

    <%= render 'shared/dropdown' do %>
      <button data-action="card#delete" class="text-left font-semibold w-full block py-2 leading-5 text-red-700 hover:bg-gray-100 hover:text-red-900 focus:outline-none focus:bg-gray-100 focus:text-red-900">
        <div class="px-4">Delete</div>
      </button>
    <% end %>
  </div>
</div>
